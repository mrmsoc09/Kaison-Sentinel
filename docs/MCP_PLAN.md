# MCP Integration Plan (Optional)

- Expose OSINT tools as MCP servers for external clients.
- Allow Kaison Sentinel to consume external MCP tools when enabled.
- Keep MCP disabled by default; allowlist required for remote hosts.

Config: `config/mcp.json`
