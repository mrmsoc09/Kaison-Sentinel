# Mitigation Module

Mitigation output is tiered: minimal, standard, deep.

Rules are defined in `config/mitigation_templates.json` and applied by keyword.
Cloud CLI guidance is included for GCP/AWS/Azure/Cloudflare.
